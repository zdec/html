# Variables para Docker Compose
# Copiar a .env: cp .env.example .env

DB_DATABASE=ecommerce
DB_USERNAME=ecommerce
DB_PASSWORD=secret

# Variables para Laravel (se pasan al contenedor app)
# APP_KEY se genera con: docker compose exec app php artisan key:generate
APP_KEY=
APP_URL=http://localhost:8080
